# import tfrun
import "tfrun" as tfrun

# List of prohibited tags
prohibited_list = ["prod", "test"]

# Get eorkspace tags from current run
tags = tfrun.tags

# Rule to detect prhibited tags
prohibited_workspace_tags = rule {
    tags not contains prohibited_list
}

main = rule {
    (prohibited_workspace_tags) else true
}
