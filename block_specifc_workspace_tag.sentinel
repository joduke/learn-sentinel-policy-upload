# import tfrun
import "tfrun" as tfrun

# List of prohibited tags
prohibited_list = ["prod", "test"]

# Get workspace tags from current run
tags = tfrun.workspace.tags

# Rule to detect prohibited tags
prohibited_workspace_tags = rule {
    tags contains prohibited_list
}

main = rule {
    (prohibited_workspace_tags) else false
}
